<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Interactive Glass UI</title>

    <style>
        /* 1. Panggil Font Inter lewat CSS @import */
        @import url('https://fonts.googleapis.com/css2?family=Lilex:wght@300;400;600;700;800&display=swap');

        body, html { 
            margin: 0; 
            padding: 0;
            height: 100%; 
            overflow: hidden; 
            background: #000; 
            /* 2. Terapkan font */
            font-family: 'Lilex'; 
        }

        /* Background bergerak */
        .bg-container {
            position: fixed;
            top: -10%; left: -10%;
            width: 120%; height: 120%;
            background: url('https://wallpapercave.com/wp/MZFt2vR.jpg') center/cover;
            transition: transform 0.1s ease-out;
            z-index: 1;
        }

        /* Container Kartu Kaca */
        .glass-card {
            position: absolute;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%);
            width: 400px;
            padding: 40px;
            border-radius: 24px;
            z-index: 2;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            color: white;
            text-align: center;
        }

        /* PENGATURAN TINGKAT BOLD (Font-Weight) */
        .glass-card h2 {
            font-weight: 800; /* Paling Tebal (Extra Bold) */
            font-size: 28px;
            margin-bottom: 10px;
            letter-spacing: -1px;
        }

        .glass-card p {
            font-weight: 300; /* Tipis (Light) */
            font-size: 18px;
            opacity: 0.8;
            margin-bottom: 25px;
        }

        button {
            font-family: 'Inter', sans-serif;
            font-weight: 600; /* Semi Bold */
            padding: 12px 35px;
            border-radius: 50px;
            border: none;
            background: white;
            color: black;
            cursor: pointer;
            transition: 0.3s;
        }

        button:hover {
            transform: scale(1.05);
            background: #f0f0f0;
        }
    </style>
</head>
<body>

    <div class="bg-container" id="bg"></div>

    <div class="glass-card">
        <h2>wongirengjembuten</h2>
        <p>si ijo dari banyumas loh yaa</p>
        <button>Berikutnya</button>
    </div>

    <script>
        const bg = document.getElementById('bg');
        window.addEventListener('mousemove', (e) => {
            let x = (e.clientX / window.innerWidth - 0.5) * 30; 
            let y = (e.clientY / window.innerHeight - 0.5) * 30;
            bg.style.transform = `translate(${x}px, ${y}px)`;
        });
    </script>

</body>
</html>
